<?xml version="1.0" encoding="utf-8"?>
<!-- Minimal plain-WebView control surface for Layer A baseline comparison.
     Not a product UX. No home chrome, no firewall chrome. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/plainRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <WebView
        android:id="@+id/plainWebView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <TextView
        android:id="@+id/plainStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:background="#AA000000"
        android:padding="8dp"
        android:text="plain-control"
        android:textColor="#FFFFFF"
        android:textSize="12sp" />
</FrameLayout>
